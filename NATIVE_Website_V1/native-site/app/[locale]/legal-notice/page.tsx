import {notFound} from 'next/navigation';import type {Metadata} from 'next';import {LegalPlaceholder} from '@/components/LegalPlaceholder';import {isLocale} from '@/lib/i18n';
type Props={params:Promise<{locale:string}>};export async function generateMetadata({params}:Props):Promise<Metadata>{const {locale}=await params;return {title:locale==='zh'?'法律声明 | NATIVE':'Legal notice | NATIVE',robots:{index:false,follow:true}}}export default async function Page({params}:Props){const {locale}=await params;if(!isLocale(locale)||locale==='de')notFound();return <LegalPlaceholder locale={locale} type="notice"/>}
