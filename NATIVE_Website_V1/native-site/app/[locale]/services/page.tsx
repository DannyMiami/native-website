import {notFound} from 'next/navigation';import {copy,isLocale} from '@/lib/i18n';import {PageHero} from '@/components/Site';
export default async function P({params}:{params:Promise<{locale:string}>}){const {locale}=await params;if(!isLocale(locale))notFound();const c=copy[locale];return <main><PageHero eyebrow="NATIVE / SERVICES" title={c.services.title} lead={c.services.lead}/><section className="content shell"><div className="grid">{c.serviceCards.map((x,i)=><div className="card" key={x[0]}><div><div className="num">0{i+1}</div><h3>{x[0]}</h3></div><p>{x[1]}</p></div>)}</div></section></main>}
