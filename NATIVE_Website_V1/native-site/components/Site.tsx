import Link from 'next/link';
import {copy, Locale} from '@/lib/i18n';
import {LanguageSwitcher} from '@/components/LanguageSwitcher';
import {ActiveLink} from '@/components/ActiveLink';

export function Header({locale}:{locale:Locale}){const c=copy[locale];return <header className="site-header shell"><nav className="nav" aria-label="Primary navigation"><ActiveLink className="brand" href={`/${locale}`}>NATIVE</ActiveLink><div className="navlinks"><ActiveLink href={`/${locale}/creators`}>{c.nav.creators}</ActiveLink><ActiveLink href={`/${locale}/brands`}>{c.nav.brands}</ActiveLink><ActiveLink href={`/${locale}/services`}>{c.nav.services}</ActiveLink><ActiveLink href={`/${locale}/cases`}>{c.nav.cases}</ActiveLink><ActiveLink href={`/${locale}/about`}>{c.nav.about}</ActiveLink><ActiveLink className="button contact-link" href={`/${locale}/contact`}>{c.nav.contact}<span aria-hidden="true">↗</span></ActiveLink><LanguageSwitcher locale={locale}/></div></nav></header>}
export function Footer({locale}:{locale:Locale}){const c=copy[locale];const legal=locale==='de'?{notice:'Impressum',noticeHref:'impressum',privacy:'Datenschutz'}:locale==='zh'?{notice:'法律声明',noticeHref:'legal-notice',privacy:'隐私政策'}:{notice:'Legal notice',noticeHref:'legal-notice',privacy:'Privacy'};return <footer className="site-footer shell"><div className="footer"><div><strong>NATIVE</strong><p>{c.footer}</p><span className="footer-contact-placeholder">Contact email — pending launch details</span></div><div className="footer-right"><Link href={`/${locale}/creators`}>{c.nav.creators}</Link><Link href={`/${locale}/brands`}>{c.nav.brands}</Link><Link href={`/${locale}/services`}>{c.nav.services}</Link><Link href={`/${locale}/cases`}>{c.nav.cases}</Link><Link href={`/${locale}/about`}>{c.nav.about}</Link><Link href={`/${locale}/contact`}>{c.nav.contact}</Link></div><div className="footer-meta"><LanguageSwitcher locale={locale}/><div><Link href={`/${locale}/${legal.noticeHref}`}>{legal.notice}</Link><Link href={`/${locale}/${locale==='de'?'datenschutz':'privacy'}`}>{legal.privacy}</Link></div></div></div></footer>}
export function PageHero({eyebrow,title,lead}:{eyebrow?:string,title:string,lead:string}){return <section className="pagehero shell">{eyebrow&&<div className="eyebrow">{eyebrow}</div>}<h1>{title}</h1><p>{lead}</p></section>}
